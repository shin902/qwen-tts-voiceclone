このリポジトリはmacOS (MacBook) 専用です。

必要ツール:
- mise（任意）
- uv
- sox

セットアップ手順:

1. `mise` のインストールと初期化（任意）

```
mise i
```

uvのインストールが済んでいたら不要です

2. `uv` の同期

```
uv sync
```

3. soxのインストール
```bash
brew install sox
```

以上でローカル環境のセットアップが完了します。